# CLAUDE.md — Central Tracking

## Project Overview

Central Tracking is a desktop task and time tracking app built with Electron, React, and TypeScript. It uses a local SQLite database (via `better-sqlite3`) and includes a CLI (`ct`) for programmatic interaction. The CLI communicates with the running Electron app via a local HTTP server, enabling AI agents and scripts to perform all operations available in the UI.

## Architecture

```
src/
  main/              # Electron main process
    main.ts          # App entry point, window creation, IPC registration, HTTP server startup
    preload.ts       # Context bridge (exposes CentralTrackingAPI to renderer)
    logger.ts        # Debug logger (--debug flag)
    database/        # SQLite database class + migrations
    ipc/             # IPC handlers: tasks, timeEntries, comments, categories, reports, import
    server/          # Local HTTP server for CLI communication
      httpServer.ts  # HTTP server with route table mapping to handler functions
      auth.ts        # Token generation, server file management, validation
    reports/         # Pure report logic (no Electron dependency)
      csvGenerator.ts
    import/          # Pure import logic (no Electron dependency)
      importExecutor.ts
      markdownParser.ts
    __tests__/       # Main process tests
  cli/               # CLI tool (`ct`)
    main.ts          # Entry point, yargs command tree
    client.ts        # HTTP client (reads ct-server.json, auth)
    formatters.ts    # Human-readable output (tables, durations, report text)
    commands/        # Command modules: task, timer, time, report, comment, category, import, status
    __tests__/       # CLI unit + integration tests
  renderer/          # React frontend (webpack-bundled)
    App.tsx          # Root component, wraps providers + HashRouter
    components/      # Layout, Sidebar, TaskList, TaskDetail, TimerBar,
                     # ReportView, DateRangePicker, SplitButton, OptionsMenu,
                     # TimeEntryEditor
    context/         # TaskContext (CRUD + filtering), TimerContext (active timer)
    utils/           # Helpers (time formatting, duration parsing, validation)
  shared/
    types.ts         # Shared TypeScript types (Task, TimeEntry, Comment, etc.)
  test/              # Test infrastructure
    setup.ts         # Global test setup
    mocks/           # Mock factories (api, electron, database)
```

- **Main ↔ Renderer communication**: IPC via `contextBridge` / `ipcRenderer.invoke`. The API shape is defined in `CentralTrackingAPI` in `src/shared/types.ts`.
- **Main ↔ CLI communication**: Local HTTP server (127.0.0.1) with bearer token auth. CLI discovers the server via `{userData}/ct-server.json`.
- **Real-time UI updates**: HTTP server fires `webContents.send('ct:data-changed')` after mutations. Renderer contexts subscribe and refresh with 100ms debounce.
- **Handler extraction**: IPC handler business logic is extracted as named exports (e.g., `createTask`, `getActiveTasks`). Both IPC registration and HTTP routes call these same functions.
- **Database**: SQLite with WAL mode, foreign keys enabled. Schema managed via sequential migrations in `src/main/database/migrations.ts`.
- **Routing**: HashRouter with `/` (tasks) and `/reports` (reporting view).

## CLI (`ct`)

The CLI enables programmatic interaction with the running app. All changes made via CLI appear in the UI in real-time.

### Usage

```bash
ct [--json] <command> [subcommand] [args] [flags]
```

### Commands

The block below is auto-generated from `ct --help`. Regenerate with `npm run docs`; `npm run docs:check` fails if it has drifted.

<!-- CT_CLI_DOCS:START -->
<!-- Auto-generated by scripts/generate-cli-docs.js. Do not edit by hand; run `npm run docs`. -->

### `ct`

```
ct [--json] [--debug] [--timeout=SECONDS] <command> [subcommand] [args]

Commands:
  ct task      Manage tasks
  ct timer     Control the timer
  ct time      Manage time entries
  ct report    Generate reports
  ct comment   Manage comments
  ct category  Manage categories
  ct import    Import tasks from file
  ct status    Show app status
  ct version   Show version
  ct plugin    Manage external plugins

Options:
      --version  Show version number                                   [boolean]
      --json     Output in JSON format                [boolean] [default: false]
      --debug    Log HTTP request/response to stderr  [boolean] [default: false]
      --timeout  Request timeout in seconds               [number] [default: 10]
  -h, --help     Show help                                             [boolean]
```

#### `ct task`

```
ct task

Manage tasks

Commands:
  ct task list                  List tasks
  ct task get <id>              Get a task by ID
  ct task create <title>        Create a new task
  ct task update <id>           Update a task
  ct task delete <ids..>        Soft-delete task(s)
  ct task restore <ids..>       Restore deleted task(s)
  ct task purge                 Permanently delete task(s)
  ct task reorder <ids..>       Set task sort order
  ct task batch-update <ids..>  Batch update tasks

Options:
      --version  Show version number                                   [boolean]
      --json     Output in JSON format                [boolean] [default: false]
      --debug    Log HTTP request/response to stderr  [boolean] [default: false]
      --timeout  Request timeout in seconds               [number] [default: 10]
  -h, --help     Show help                                             [boolean]
```

##### `ct task list`

```
ct task list

List tasks

Options:
      --version   Show version number                                  [boolean]
      --json      Output in JSON format               [boolean] [default: false]
      --debug     Log HTTP request/response to stderr [boolean] [default: false]
      --timeout   Request timeout in seconds              [number] [default: 10]
      --done      Show completed tasks                                 [boolean]
      --deleted   Show deleted tasks (recycle bin)                     [boolean]
      --all       Show all tasks                                       [boolean]
  -s, --search    Search title and description                          [string]
      --status    Filter by status                                      [string]
      --source    Filter by source                                      [string]
      --category  Filter by category ID                                 [string]
      --sort      Sort: manual|recent|created|alphabetical|most-time-today
                                                                        [string]
      --limit     Max results                             [number] [default: 50]
      --offset    Skip results                             [number] [default: 0]
      --full-id   Show full UUID instead of prefix    [boolean] [default: false]
  -h, --help      Show help                                            [boolean]
```

##### `ct task get`

```
ct task get <id>

Get a task by ID

Positionals:
  id  UUID, prefix, or name substring                        [string] [required]

Options:
      --version  Show version number                                   [boolean]
      --json     Output in JSON format                [boolean] [default: false]
      --debug    Log HTTP request/response to stderr  [boolean] [default: false]
      --timeout  Request timeout in seconds               [number] [default: 10]
  -h, --help     Show help                                             [boolean]
```

##### `ct task create`

```
ct task create <title>

Create a new task

Positionals:
  title                                                      [string] [required]

Options:
      --version      Show version number                               [boolean]
      --json         Output in JSON format            [boolean] [default: false]
      --debug        Log HTTP request/response to stderr
                                                      [boolean] [default: false]
      --timeout      Request timeout in seconds           [number] [default: 10]
  -d, --description                                                     [string]
      --status      [string] [choices: "todo", "in-progress", "done", "blocked"]
      --source   [string] [choices: "ad-hoc", "email", "meeting-prep", "plugin"]
      --category     Category ID(s)                                      [array]
  -h, --help         Show help                                         [boolean]
```

##### `ct task update`

```
ct task update <id>

Update a task

Positionals:
  id  UUID, prefix, or name substring                        [string] [required]

Options:
      --version      Show version number                               [boolean]
      --json         Output in JSON format            [boolean] [default: false]
      --debug        Log HTTP request/response to stderr
                                                      [boolean] [default: false]
      --timeout      Request timeout in seconds           [number] [default: 10]
      --title                                                           [string]
  -d, --description                                                     [string]
      --status      [string] [choices: "todo", "in-progress", "done", "blocked"]
      --source   [string] [choices: "ad-hoc", "email", "meeting-prep", "plugin"]
      --notes                                                           [string]
      --category     Category ID(s)                                      [array]
  -h, --help         Show help                                         [boolean]
```

##### `ct task delete`

```
ct task delete <ids..>

Soft-delete task(s)

Positionals:
  ids  UUID(s), prefix(es), or name(s)          [array] [required] [default: []]

Options:
      --version  Show version number                                   [boolean]
      --json     Output in JSON format                [boolean] [default: false]
      --debug    Log HTTP request/response to stderr  [boolean] [default: false]
      --timeout  Request timeout in seconds               [number] [default: 10]
  -h, --help     Show help                                             [boolean]
```

##### `ct task restore`

```
ct task restore <ids..>

Restore deleted task(s)

Positionals:
  ids  UUID(s), prefix(es), or name(s)          [array] [required] [default: []]

Options:
      --version  Show version number                                   [boolean]
      --json     Output in JSON format                [boolean] [default: false]
      --debug    Log HTTP request/response to stderr  [boolean] [default: false]
      --timeout  Request timeout in seconds               [number] [default: 10]
  -h, --help     Show help                                             [boolean]
```

##### `ct task purge`

```
ct task purge

Permanently delete task(s)

Options:
      --version  Show version number                                   [boolean]
      --json     Output in JSON format                [boolean] [default: false]
      --debug    Log HTTP request/response to stderr  [boolean] [default: false]
      --timeout  Request timeout in seconds               [number] [default: 10]
      --all      Empty recycle bin                                     [boolean]
      --id       UUID, prefix, or name substring                        [string]
  -h, --help     Show help                                             [boolean]
```

##### `ct task reorder`

```
ct task reorder <ids..>

Set task sort order

Positionals:
  ids  UUID(s), prefix(es), or name(s)          [array] [required] [default: []]

Options:
      --version  Show version number                                   [boolean]
      --json     Output in JSON format                [boolean] [default: false]
      --debug    Log HTTP request/response to stderr  [boolean] [default: false]
      --timeout  Request timeout in seconds               [number] [default: 10]
  -h, --help     Show help                                             [boolean]
```

##### `ct task batch-update`

```
ct task batch-update <ids..>

Batch update tasks

Positionals:
  ids  UUID(s), prefix(es), or name(s)          [array] [required] [default: []]

Options:
      --version   Show version number                                  [boolean]
      --json      Output in JSON format               [boolean] [default: false]
      --debug     Log HTTP request/response to stderr [boolean] [default: false]
      --timeout   Request timeout in seconds              [number] [default: 10]
      --status      [string] [choices: "todo", "in-progress", "done", "blocked"]
      --source   [string] [choices: "ad-hoc", "email", "meeting-prep", "plugin"]
      --category                                                         [array]
  -h, --help      Show help                                            [boolean]
```

#### `ct timer`

```
ct timer

Control the timer

Commands:
  ct timer start <task-id>  Start timer for a task
  ct timer stop             Stop the active timer
  ct timer status           Show active timer

Options:
      --version  Show version number                                   [boolean]
      --json     Output in JSON format                [boolean] [default: false]
      --debug    Log HTTP request/response to stderr  [boolean] [default: false]
      --timeout  Request timeout in seconds               [number] [default: 10]
  -h, --help     Show help                                             [boolean]
```

##### `ct timer start`

```
ct timer start <task-id>

Start timer for a task

Positionals:
  task-id  UUID, prefix, or name substring                   [string] [required]

Options:
      --version  Show version number                                   [boolean]
      --json     Output in JSON format                [boolean] [default: false]
      --debug    Log HTTP request/response to stderr  [boolean] [default: false]
      --timeout  Request timeout in seconds               [number] [default: 10]
  -h, --help     Show help                                             [boolean]
```

##### `ct timer stop`

```
ct timer stop

Stop the active timer

Options:
      --version  Show version number                                   [boolean]
      --json     Output in JSON format                [boolean] [default: false]
      --debug    Log HTTP request/response to stderr  [boolean] [default: false]
      --timeout  Request timeout in seconds               [number] [default: 10]
  -h, --help     Show help                                             [boolean]
```

##### `ct timer status`

```
ct timer status

Show active timer

Options:
      --version  Show version number                                   [boolean]
      --json     Output in JSON format                [boolean] [default: false]
      --debug    Log HTTP request/response to stderr  [boolean] [default: false]
      --timeout  Request timeout in seconds               [number] [default: 10]
  -h, --help     Show help                                             [boolean]
```

#### `ct time`

```
ct time

Manage time entries

Commands:
  ct time list <task-id>  List time entries for a task
  ct time add <task-id>   Add a manual time entry
  ct time update <id>     Update a time entry
  ct time delete <id>     Delete a time entry
  ct time today           Show total time tracked today

Options:
      --version  Show version number                                   [boolean]
      --json     Output in JSON format                [boolean] [default: false]
      --debug    Log HTTP request/response to stderr  [boolean] [default: false]
      --timeout  Request timeout in seconds               [number] [default: 10]
  -h, --help     Show help                                             [boolean]
```

##### `ct time list`

```
ct time list <task-id>

List time entries for a task

Positionals:
  task-id  UUID, prefix, or name substring                   [string] [required]

Options:
      --version  Show version number                                   [boolean]
      --json     Output in JSON format                [boolean] [default: false]
      --debug    Log HTTP request/response to stderr  [boolean] [default: false]
      --timeout  Request timeout in seconds               [number] [default: 10]
      --limit                                             [number] [default: 20]
      --offset                                             [number] [default: 0]
  -h, --help     Show help                                             [boolean]
```

##### `ct time add`

```
ct time add <task-id>

Add a manual time entry

Positionals:
  task-id  UUID, prefix, or name substring                   [string] [required]

Options:
      --version   Show version number                                  [boolean]
      --json      Output in JSON format               [boolean] [default: false]
      --debug     Log HTTP request/response to stderr [boolean] [default: false]
      --timeout   Request timeout in seconds              [number] [default: 10]
      --start     Start time (ISO 8601)                                 [string]
      --end       End time (ISO 8601)                                   [string]
      --duration  Duration (e.g., 1h30m, 1:30, 90)                      [string]
      --note                                              [string] [default: ""]
  -h, --help      Show help                                            [boolean]
```

##### `ct time update`

```
ct time update <id>

Update a time entry

Positionals:
  id                                                         [string] [required]

Options:
      --version  Show version number                                   [boolean]
      --json     Output in JSON format                [boolean] [default: false]
      --debug    Log HTTP request/response to stderr  [boolean] [default: false]
      --timeout  Request timeout in seconds               [number] [default: 10]
      --start                                                           [string]
      --end                                                             [string]
      --note                                                            [string]
  -h, --help     Show help                                             [boolean]
```

##### `ct time delete`

```
ct time delete <id>

Delete a time entry

Positionals:
  id                                                         [string] [required]

Options:
      --version  Show version number                                   [boolean]
      --json     Output in JSON format                [boolean] [default: false]
      --debug    Log HTTP request/response to stderr  [boolean] [default: false]
      --timeout  Request timeout in seconds               [number] [default: 10]
  -h, --help     Show help                                             [boolean]
```

##### `ct time today`

```
ct time today

Show total time tracked today

Options:
      --version  Show version number                                   [boolean]
      --json     Output in JSON format                [boolean] [default: false]
      --debug    Log HTTP request/response to stderr  [boolean] [default: false]
      --timeout  Request timeout in seconds               [number] [default: 10]
  -h, --help     Show help                                             [boolean]
```

#### `ct report`

```
ct report

Generate reports

Commands:
  ct report summary  Plain text summary report grouped by date
  ct report detail   Detailed per-entry report
  ct report chart    Chart data (date x task x seconds)
  ct report export   Export CSV report

Options:
      --version  Show version number                                   [boolean]
      --json     Output in JSON format                [boolean] [default: false]
      --debug    Log HTTP request/response to stderr  [boolean] [default: false]
      --timeout  Request timeout in seconds               [number] [default: 10]
  -h, --help     Show help                                             [boolean]
```

##### `ct report summary`

```
ct report summary

Plain text summary report grouped by date

Options:
      --version  Show version number                                   [boolean]
      --json     Output in JSON format                [boolean] [default: false]
      --debug    Log HTTP request/response to stderr  [boolean] [default: false]
      --timeout  Request timeout in seconds               [number] [default: 10]
      --from     Start date (YYYY-MM-DD)                     [string] [required]
      --to       End date (YYYY-MM-DD)                       [string] [required]
  -h, --help     Show help                                             [boolean]
```

##### `ct report detail`

```
ct report detail

Detailed per-entry report

Options:
      --version  Show version number                                   [boolean]
      --json     Output in JSON format                [boolean] [default: false]
      --debug    Log HTTP request/response to stderr  [boolean] [default: false]
      --timeout  Request timeout in seconds               [number] [default: 10]
      --from                                                 [string] [required]
      --to                                                   [string] [required]
  -h, --help     Show help                                             [boolean]
```

##### `ct report chart`

```
ct report chart

Chart data (date x task x seconds)

Options:
      --version  Show version number                                   [boolean]
      --json     Output in JSON format                [boolean] [default: false]
      --debug    Log HTTP request/response to stderr  [boolean] [default: false]
      --timeout  Request timeout in seconds               [number] [default: 10]
      --from                                                 [string] [required]
      --to                                                   [string] [required]
  -h, --help     Show help                                             [boolean]
```

##### `ct report export`

```
ct report export

Export CSV report

Options:
      --version  Show version number                                   [boolean]
      --json     Output in JSON format                [boolean] [default: false]
      --debug    Log HTTP request/response to stderr  [boolean] [default: false]
      --timeout  Request timeout in seconds               [number] [default: 10]
      --from                                                 [string] [required]
      --to                                                   [string] [required]
      --out      Output file path (default: stdout)                     [string]
  -h, --help     Show help                                             [boolean]
```

#### `ct comment`

```
ct comment

Manage comments

Commands:
  ct comment list <task-id>        List comments for a task
  ct comment add <task-id> <body>  Add a comment to a task
  ct comment update <id>           Update a comment
  ct comment delete <id>           Delete a comment

Options:
      --version  Show version number                                   [boolean]
      --json     Output in JSON format                [boolean] [default: false]
      --debug    Log HTTP request/response to stderr  [boolean] [default: false]
      --timeout  Request timeout in seconds               [number] [default: 10]
  -h, --help     Show help                                             [boolean]
```

##### `ct comment list`

```
ct comment list <task-id>

List comments for a task

Positionals:
  task-id  UUID, prefix, or name substring                   [string] [required]

Options:
      --version  Show version number                                   [boolean]
      --json     Output in JSON format                [boolean] [default: false]
      --debug    Log HTTP request/response to stderr  [boolean] [default: false]
      --timeout  Request timeout in seconds               [number] [default: 10]
  -h, --help     Show help                                             [boolean]
```

##### `ct comment add`

```
ct comment add <task-id> <body>

Add a comment to a task

Positionals:
  task-id  UUID, prefix, or name substring                   [string] [required]
  body                                                       [string] [required]

Options:
      --version   Show version number                                  [boolean]
      --json      Output in JSON format               [boolean] [default: false]
      --debug     Log HTTP request/response to stderr [boolean] [default: false]
      --timeout   Request timeout in seconds              [number] [default: 10]
      --syncable                                      [boolean] [default: false]
  -h, --help      Show help                                            [boolean]
```

##### `ct comment update`

```
ct comment update <id>

Update a comment

Positionals:
  id                                                         [string] [required]

Options:
      --version   Show version number                                  [boolean]
      --json      Output in JSON format               [boolean] [default: false]
      --debug     Log HTTP request/response to stderr [boolean] [default: false]
      --timeout   Request timeout in seconds              [number] [default: 10]
      --body                                                            [string]
      --syncable                                                       [boolean]
      --synced    Mark as synced to the external system                [boolean]
  -h, --help      Show help                                            [boolean]
```

##### `ct comment delete`

```
ct comment delete <id>

Delete a comment

Positionals:
  id                                                         [string] [required]

Options:
      --version  Show version number                                   [boolean]
      --json     Output in JSON format                [boolean] [default: false]
      --debug    Log HTTP request/response to stderr  [boolean] [default: false]
      --timeout  Request timeout in seconds               [number] [default: 10]
  -h, --help     Show help                                             [boolean]
```

#### `ct category`

```
ct category

Manage categories

Commands:
  ct category list                          List all categories
  ct category create <name>                 Create a category
  ct category update <id>                   Update a category
  ct category delete <id>                   Delete a category
  ct category assign <task-id>              Assign categories to a task
  <category-ids..>

Options:
      --version  Show version number                                   [boolean]
      --json     Output in JSON format                [boolean] [default: false]
      --debug    Log HTTP request/response to stderr  [boolean] [default: false]
      --timeout  Request timeout in seconds               [number] [default: 10]
  -h, --help     Show help                                             [boolean]
```

##### `ct category list`

```
ct category list

List all categories

Options:
      --version  Show version number                                   [boolean]
      --json     Output in JSON format                [boolean] [default: false]
      --debug    Log HTTP request/response to stderr  [boolean] [default: false]
      --timeout  Request timeout in seconds               [number] [default: 10]
  -h, --help     Show help                                             [boolean]
```

##### `ct category create`

```
ct category create <name>

Create a category

Positionals:
  name                                                       [string] [required]

Options:
      --version  Show version number                                   [boolean]
      --json     Output in JSON format                [boolean] [default: false]
      --debug    Log HTTP request/response to stderr  [boolean] [default: false]
      --timeout  Request timeout in seconds               [number] [default: 10]
      --color    Hex color (e.g., #ff0000)         [string] [default: "#888888"]
  -h, --help     Show help                                             [boolean]
```

##### `ct category update`

```
ct category update <id>

Update a category

Positionals:
  id                                                         [string] [required]

Options:
      --version  Show version number                                   [boolean]
      --json     Output in JSON format                [boolean] [default: false]
      --debug    Log HTTP request/response to stderr  [boolean] [default: false]
      --timeout  Request timeout in seconds               [number] [default: 10]
      --name                                                            [string]
      --color                                                           [string]
  -h, --help     Show help                                             [boolean]
```

##### `ct category delete`

```
ct category delete <id>

Delete a category

Positionals:
  id                                                         [string] [required]

Options:
      --version  Show version number                                   [boolean]
      --json     Output in JSON format                [boolean] [default: false]
      --debug    Log HTTP request/response to stderr  [boolean] [default: false]
      --timeout  Request timeout in seconds               [number] [default: 10]
  -h, --help     Show help                                             [boolean]
```

##### `ct category assign`

```
ct category assign <task-id> <category-ids..>

Assign categories to a task

Positionals:
  task-id       UUID, prefix, or name substring              [string] [required]
  category-ids                                  [array] [required] [default: []]

Options:
      --version  Show version number                                   [boolean]
      --json     Output in JSON format                [boolean] [default: false]
      --debug    Log HTTP request/response to stderr  [boolean] [default: false]
      --timeout  Request timeout in seconds               [number] [default: 10]
  -h, --help     Show help                                             [boolean]
```

#### `ct import`

```
ct import

Import tasks from file

Commands:
  ct import preview <file>  Parse import file and show preview
  ct import execute <file>  Execute import from file

Options:
      --version  Show version number                                   [boolean]
      --json     Output in JSON format                [boolean] [default: false]
      --debug    Log HTTP request/response to stderr  [boolean] [default: false]
      --timeout  Request timeout in seconds               [number] [default: 10]
  -h, --help     Show help                                             [boolean]
```

##### `ct import preview`

```
ct import preview <file>

Parse import file and show preview

Positionals:
  file                                                       [string] [required]

Options:
      --version  Show version number                                   [boolean]
      --json     Output in JSON format                [boolean] [default: false]
      --debug    Log HTTP request/response to stderr  [boolean] [default: false]
      --timeout  Request timeout in seconds               [number] [default: 10]
  -h, --help     Show help                                             [boolean]
```

##### `ct import execute`

```
ct import execute <file>

Execute import from file

Positionals:
  file                                                       [string] [required]

Options:
      --version          Show version number                           [boolean]
      --json             Output in JSON format        [boolean] [default: false]
      --debug            Log HTTP request/response to stderr
                                                      [boolean] [default: false]
      --timeout          Request timeout in seconds       [number] [default: 10]
      --skip-existing    Skip items that match existing tasks
                                                       [boolean] [default: true]
      --update-existing  Update existing tasks instead of skipping
                                                      [boolean] [default: false]
      --dry-run          Preview without executing    [boolean] [default: false]
  -h, --help             Show help                                     [boolean]
```

#### `ct status`

```
ct status

Show app status

Options:
      --version  Show version number                                   [boolean]
      --json     Output in JSON format                [boolean] [default: false]
      --debug    Log HTTP request/response to stderr  [boolean] [default: false]
      --timeout  Request timeout in seconds               [number] [default: 10]
  -h, --help     Show help                                             [boolean]
```

#### `ct version`

```
ct version

Show version

Options:
      --version  Show version number                                   [boolean]
      --json     Output in JSON format                [boolean] [default: false]
      --debug    Log HTTP request/response to stderr  [boolean] [default: false]
      --timeout  Request timeout in seconds               [number] [default: 10]
  -h, --help     Show help                                             [boolean]
```

#### `ct plugin`

```
ct plugin

Manage external plugins

Commands:
  ct plugin install <manifest-file>  Install or update a plugin from a manifest
                                     JSON file
  ct plugin list                     List installed plugins
  ct plugin enable <id>              Enable a plugin (webhook subscriptions
                                     become active)
  ct plugin disable <id>             Disable a plugin (stops receiving events)
  ct plugin uninstall <id>           Remove a plugin and its config
  ct plugin config                   Get or set plugin configuration
  ct plugin run <id>                 Spawn a plugin's entrypoint with CT_* env
                                     vars

Options:
      --version  Show version number                                   [boolean]
      --json     Output in JSON format                [boolean] [default: false]
      --debug    Log HTTP request/response to stderr  [boolean] [default: false]
      --timeout  Request timeout in seconds               [number] [default: 10]
  -h, --help     Show help                                             [boolean]
```

##### `ct plugin install`

```
ct plugin install <manifest-file>

Install or update a plugin from a manifest JSON file

Positionals:
  manifest-file                                              [string] [required]

Options:
      --version  Show version number                                   [boolean]
      --json     Output in JSON format                [boolean] [default: false]
      --debug    Log HTTP request/response to stderr  [boolean] [default: false]
      --timeout  Request timeout in seconds               [number] [default: 10]
  -h, --help     Show help                                             [boolean]
```

##### `ct plugin list`

```
ct plugin list

List installed plugins

Options:
      --version  Show version number                                   [boolean]
      --json     Output in JSON format                [boolean] [default: false]
      --debug    Log HTTP request/response to stderr  [boolean] [default: false]
      --timeout  Request timeout in seconds               [number] [default: 10]
  -h, --help     Show help                                             [boolean]
```

##### `ct plugin enable`

```
ct plugin enable <id>

Enable a plugin (webhook subscriptions become active)

Positionals:
  id                                                         [string] [required]

Options:
      --version  Show version number                                   [boolean]
      --json     Output in JSON format                [boolean] [default: false]
      --debug    Log HTTP request/response to stderr  [boolean] [default: false]
      --timeout  Request timeout in seconds               [number] [default: 10]
  -h, --help     Show help                                             [boolean]
```

##### `ct plugin disable`

```
ct plugin disable <id>

Disable a plugin (stops receiving events)

Positionals:
  id                                                         [string] [required]

Options:
      --version  Show version number                                   [boolean]
      --json     Output in JSON format                [boolean] [default: false]
      --debug    Log HTTP request/response to stderr  [boolean] [default: false]
      --timeout  Request timeout in seconds               [number] [default: 10]
  -h, --help     Show help                                             [boolean]
```

##### `ct plugin uninstall`

```
ct plugin uninstall <id>

Remove a plugin and its config

Positionals:
  id                                                         [string] [required]

Options:
      --version  Show version number                                   [boolean]
      --json     Output in JSON format                [boolean] [default: false]
      --debug    Log HTTP request/response to stderr  [boolean] [default: false]
      --timeout  Request timeout in seconds               [number] [default: 10]
  -h, --help     Show help                                             [boolean]
```

##### `ct plugin config`

```
ct plugin config

Get or set plugin configuration

Commands:
  ct plugin config get <id> <key>          Read a plugin config value
  ct plugin config set <id> <key> <value>  Write a plugin config value
  ct plugin config list <id>               List all config keys for a plugin
  ct plugin config delete <id> <key>       Delete a plugin config key

Options:
      --version  Show version number                                   [boolean]
      --json     Output in JSON format                [boolean] [default: false]
      --debug    Log HTTP request/response to stderr  [boolean] [default: false]
      --timeout  Request timeout in seconds               [number] [default: 10]
  -h, --help     Show help                                             [boolean]
```

###### `ct plugin config get`

```
ct plugin config get <id> <key>

Read a plugin config value

Positionals:
  id                                                         [string] [required]
  key                                                        [string] [required]

Options:
      --version  Show version number                                   [boolean]
      --json     Output in JSON format                [boolean] [default: false]
      --debug    Log HTTP request/response to stderr  [boolean] [default: false]
      --timeout  Request timeout in seconds               [number] [default: 10]
  -h, --help     Show help                                             [boolean]
```

###### `ct plugin config set`

```
ct plugin config set <id> <key> <value>

Write a plugin config value

Positionals:
  id                                                         [string] [required]
  key                                                        [string] [required]
  value                                                      [string] [required]

Options:
      --version  Show version number                                   [boolean]
      --json     Output in JSON format                [boolean] [default: false]
      --debug    Log HTTP request/response to stderr  [boolean] [default: false]
      --timeout  Request timeout in seconds               [number] [default: 10]
  -h, --help     Show help                                             [boolean]
```

###### `ct plugin config list`

```
ct plugin config list <id>

List all config keys for a plugin

Positionals:
  id                                                         [string] [required]

Options:
      --version  Show version number                                   [boolean]
      --json     Output in JSON format                [boolean] [default: false]
      --debug    Log HTTP request/response to stderr  [boolean] [default: false]
      --timeout  Request timeout in seconds               [number] [default: 10]
  -h, --help     Show help                                             [boolean]
```

###### `ct plugin config delete`

```
ct plugin config delete <id> <key>

Delete a plugin config key

Positionals:
  id                                                         [string] [required]
  key                                                        [string] [required]

Options:
      --version  Show version number                                   [boolean]
      --json     Output in JSON format                [boolean] [default: false]
      --debug    Log HTTP request/response to stderr  [boolean] [default: false]
      --timeout  Request timeout in seconds               [number] [default: 10]
  -h, --help     Show help                                             [boolean]
```

##### `ct plugin run`

```
ct plugin run <id>

Spawn a plugin's entrypoint with CT_* env vars

Positionals:
  id                                                         [string] [required]

Options:
      --version  Show version number                                   [boolean]
      --json     Output in JSON format                [boolean] [default: false]
      --debug    Log HTTP request/response to stderr  [boolean] [default: false]
      --timeout  Request timeout in seconds               [number] [default: 10]
  -h, --help     Show help                                             [boolean]
```

<!-- CT_CLI_DOCS:END -->

### Building the CLI

```bash
npm run build:cli    # Compiles CLI to dist/cli/
npm run build        # Full build (main + renderer + CLI)
```

### Server Discovery

On startup, the Electron app writes `{userData}/ct-server.json` containing `{ port, token, pid }`. The CLI reads this file to connect. If the file is missing or the PID is dead, the CLI reports "App not running."

### Security

- Server binds to `127.0.0.1` only (no network exposure)
- Random UUID token per session, required via `Authorization: Bearer <token>`
- Host header validation (DNS rebinding protection)
- No CORS headers (prevents browser-based attacks)
- 1MB request body limit
- Token rotates on each app restart

### Plugins

External integrations run as separate processes and interact with the app
through the CLI plus optional webhook subscriptions.

**Plugin manifest (`plugin.json`):**

```json
{
  "id": "ado",
  "name": "Azure DevOps Sync",
  "version": "0.1.0",
  "entrypoint": "node sync.js",
  "events": ["task.created", "task.updated", "timeEntry.created"],
  "webhook": { "url": "http://127.0.0.1:9901/ct-events" }
}
```

Webhook URLs must be loopback-only (`127.0.0.1`, `localhost`, or `[::1]`).
Events are the `event` fields declared in `src/main/server/apiManifest.ts`
(e.g. `task.created`, `timeEntry.stopped`, `category.deleted`). Use `"*"` to
match everything.

**Lifecycle:**

```bash
ct plugin install ./plugin.json   # persists manifest in the plugins table
ct plugin list                    # shows id/version/enabled/webhook
ct plugin disable <id>            # stops webhook delivery
ct plugin enable <id>
ct plugin uninstall <id>          # also clears the plugin's config
```

**Configuration** (uses the `plugin_config` table):

```bash
ct plugin config set ado api-key $ADO_TOKEN
ct plugin config get ado api-key
ct plugin config list ado
```

**Running a plugin:**

```bash
ct plugin run ado
```

`ct plugin run` spawns the manifest's `entrypoint` with env vars:
`CT_PLUGIN_ID`, `CT_SERVER_URL` (loopback), and `CT_TOKEN` (session bearer
token). The plugin can call back into the HTTP API with that token, and
verify incoming webhook signatures with the same token.

**Webhook payloads** are JSON with shape
`{ event, route, data, timestamp }` and carry these headers:

- `X-CT-Signature: sha256=<hex>` — HMAC of the body using the session token
- `X-CT-Plugin-Id: <id>`

Delivery is best-effort: failures are logged to the main process stderr and
never block the originating mutation.

**Alternative (no plugin install required):** scripts can drive the CLI
directly for one-shot syncs.

```bash
external-tool list --json | ct import execute - --update-existing
ct task create "External Item" --source plugin
ct time add <task-id> --duration 2h
```

## Development

```bash
# Install dependencies
npm install

# Development mode (two terminals):
npm run dev              # Compiles main + starts webpack dev server on :3000
NODE_ENV=development npm start   # Launches Electron pointing at localhost:3000

# Production build:
npm run build            # Compiles both main and renderer
npm start                # Launches Electron loading from dist/

# Debug mode (with verbose logging):
npm run start:debug      # Launches with --debug flag
```

### Key scripts

| Script | What it does |
|---|---|
| `npm run dev` | Concurrently watches main TS + serves renderer at :3000 |
| `npm run build` | Full production build (main + renderer + CLI) |
| `npm run build:main` | Compiles main process TypeScript |
| `npm run build:cli` | Compiles CLI TypeScript |
| `npm start` | Launches Electron (`dist/main/main.js`) |
| `npm run start:debug` | Launches with `--debug` flag for verbose logging |
| `npm test` | Runs all tests via Vitest |
| `npm run test:watch` | Runs Vitest in watch mode |
| `npm run test:coverage` | Runs tests with coverage report |
| `npm run lint` | ESLint across `src/**/*.{ts,tsx}` |

### Important: NODE_ENV

When running in dev mode, Electron must be started with `NODE_ENV=development` so `main.ts` loads `http://localhost:3000` instead of the built file.

## Code Conventions

- TypeScript strict mode
- React functional components with hooks
- Context-based state management (no Redux)
- CSS files co-located with components (e.g., `Layout.css` next to `Layout.tsx`)
- IPC handlers are organized by domain in `src/main/ipc/`
- Handler business logic extracted as named exports for reuse by HTTP server
- Electron-free modules (`reports/csvGenerator.ts`, `import/importExecutor.ts`) for logic shared between IPC and HTTP
- UUIDs for all entity IDs (via `uuid` package)
- SQLite column names use `snake_case`; TypeScript types use `camelCase`

## Database

- Located at `{userData}/central-tracking.db` (Electron's `app.getPath('userData')`)
- Tables: `tasks`, `time_entries`, `comments`, `categories`, `task_categories`, `plugin_config`, `schema_version`
- Migrations are sequential SQL strings in `src/main/database/migrations.ts`
- **Migration 001**: Initial schema (all tables)
- **Migration 002**: `ALTER TABLE tasks ADD COLUMN notes TEXT NOT NULL DEFAULT '';`

## IPC API Surface

| Channel | Description |
|---|---|
| `tasks:*` | Task CRUD (getAll, getById, create, update, delete, reorder, batch ops) |
| `timeEntries:*` | Time entry CRUD + singleton timer (getByTask, create, update, delete, getActive, stopActive) |
| `timeEntries:getTodayTotal` | Today's aggregate time |
| `timeEntries:getByDateRange`, `timeEntries:getReport` | Reporting queries |
| `comments:*` | Comment CRUD |
| `categories:*` | Category CRUD + assignToTask |
| `reports:exportCsv` | CSV export with save dialog |
| `window:setAlwaysOnTop`, `window:getAlwaysOnTop` | Window management |

## HTTP API Surface

All endpoints: `POST /api/{domain}/{operation}` with JSON body `{ "args": [...] }`.

Response: `{ "ok": true, "data": <result> }` or `{ "ok": false, "error": { "code": "...", "message": "..." } }`.

The route table in `src/main/server/httpServer.ts` maps 1:1 to the extracted handler functions. Each route is tagged with `mutates: boolean` to trigger UI refresh notifications.

## Testing

- **Framework**: Vitest with jsdom environment
- **Libraries**: @testing-library/react, @testing-library/jest-dom, @testing-library/user-event
- **Test location**: `__tests__/` directories alongside source, with `.test.ts`/`.test.tsx` extensions
- **Mock infrastructure**: `src/test/mocks/` — api.ts (IPC bridge mock), electron.ts (IPC main mock), database.ts (in-memory SQLite)
- **Test layers**: IPC handler tests, HTTP server tests, CLI formatter tests, integration tests (CLI → server → database)

### Commands

```bash
npm test              # Run all tests once
npm run test:watch    # Watch mode
npm run test:coverage # Coverage report
```
